[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# 这些脚本是手动冒烟脚本（依赖外部服务：MySQL/Milvus/Redis/LLM API），不在 CI 自动跑
# 如需 pytest 一键执行，需先启动 docker-compose up 并配置 .env
addopts = -v --tb=short
